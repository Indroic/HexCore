# HexCore — Documentation

<p align="center">
  <b>A reusable core for Python applications built on hexagonal architecture, DDD, CQRS and
  background tasks — and the TypeScript client for its identity module.</b>
</p>

This is the documentation for the whole [HexCore monorepo](https://github.com/Indroic/HexCore).
Both packages are documented here, each one in English and Spanish.

---

## The two packages

| | Documentation | Package |
| :-- | :-- | :-- |
| 🐍 **HexCore** — the Python meta-framework | 🇬🇧 **[English](./hexcore/en/)** · 🇪🇸 **[Español](./hexcore/es/)** | [`hexcore` on PyPI](https://pypi.org/project/hexcore/) · [`packages/hexcore/`](../packages/hexcore/) |
| 🟦 **Darwin Client** — the agnostic TypeScript client | 🇬🇧 **[English](./darwin-client/en/)** · 🇪🇸 **[Español](./darwin-client/es/)** | [`@hexcore-js/darwin-client` on npm](https://www.npmjs.com/package/@hexcore-js/darwin-client) · [`packages/darwin-client/`](../packages/darwin-client/) |

The two are designed to be used together, but neither requires the other: HexCore is a complete
Python framework whether or not any TypeScript ever talks to it, and the client speaks Darwin's
HTTP contract rather than importing anything from the Python side.

### Where to start

| If you are… | Start at |
| :-- | :-- |
| Building a Python backend | [Installation](./hexcore/en/installation.md) → [Quickstart](./hexcore/en/quickstart.md) |
| Adding authentication to that backend | [Darwin: introduction](./hexcore/en/darwin/) |
| Building the frontend that talks to Darwin | [Darwin Client quickstart](./darwin-client/en/quickstart.md) |
| Contributing to either package | [`CONTRIBUTING.md`](../CONTRIBUTING.md) |

---

## Other documents

| Document | Language | Covers |
| :-- | :-- | :-- |
| [ARCHITECTURE_TYPING.md](./ARCHITECTURE_TYPING.md) | EN | The Python type system, the generated stubs and the CI gate. The contract for contributing. |
| [../CONTRIBUTING.md](../CONTRIBUTING.md) | EN | How to contribute, and the two release pipelines. |
| [../SECURITY.md](../SECURITY.md) | EN | Security policy and supported versions. |
| [../CODE_OF_CONDUCT.md](../CODE_OF_CONDUCT.md) | EN | Code of conduct. |
| [../CHANGELOG.md](../CHANGELOG.md) | ES | Change history of the Python package. Generated by `commitizen` from the commit subjects, which are written in Spanish. |
| [../packages/darwin-client/CHANGELOG.md](../packages/darwin-client/CHANGELOG.md) | ES | Change history of the TypeScript client. |

---

## The rule of this documentation

**If a document shows code, there is a test that runs it.**

`packages/hexcore/tests/test_documentation_examples.py` executes the startup examples, verifies
that every `from hexcore… import …` in these documents resolves against the real API, and that
any attribute asked of a facade is in its `__all__`. The example plugin from the extension guide
lives exercised in `packages/hexcore/tests/test_darwin_custom_plugin.py`.

The walk is automatic: the test lists `docs/hexcore/**/*.md`, so a new guide enters the gate
without anyone adding it to a list — which is exactly the file that would otherwise drift
without anyone noticing.

A guide nobody runs ages silently: the day an extension point changes signature, the document
keeps saying the old thing, and the first to find out is someone who has already written half a
plugin following it.

What does **not** live here is the story of how each thing was built. That is in the git
history, which is where history goes.

---

## English is the reference version

Both packages are documented in English first; `es/` is the translation. If the two ever
contradict each other, English wins. A documentation change that only touches one language
leaves the other one lying — if you change a guide, change both, or say so in the pull request
so somebody else can.
